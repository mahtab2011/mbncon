{
  "name": "@mbncon/entitlement-api",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1: central entitlement foundation for OptiFabric and OptiSewing. Service-layer only in this phase — no HTTP surface is exposed yet (see CENTRAL-ENTITLEMENT-PHASE-1.md).",
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "jest",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "prisma": "^5.0.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.0"
  }
}
